import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0x00eaefef);
  static const card = Color(0x00bfc9d1);
  static const foreground = Color(0x0025343f);
  static const primary = Color(0x00ff9b51);

  // Gradient
  static const gradientStart = Color(0x00ffb96e);
  static const gradientEnd = Color(0x00ff9b51);
}
